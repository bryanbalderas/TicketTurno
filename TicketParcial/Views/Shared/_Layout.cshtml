@using Microsoft.AspNetCore.Identity
@using TicketParcial.Areas.Identity.Data

@inject SignInManager<TicketParcialUser> SignInManager
@inject UserManager<TicketParcialUser> UserManager

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - TicketParcial</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container">
                <div class="dropdown">
                    <button class="btn bg-transparent" type="button" id="dropdown-ticket-turno" data-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-list mr-1"></i>
                        Menu
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdown-ticket-turno">
                        <h6 class="dropdown-header text-center">Tickets de turno</h6>
                        <div class="dropdown-divider"></div>
                        @if (SignInManager.IsSignedIn(User))
                        {
                            <a class="text-center nav-link text-dark" asp-area="" asp-controller="TicketTurno" asp-action="Listadmin">
                                Buscar ticket
                            </a>
                        }
                        else
                        {
                            <a class="text-center nav-link text-dark" asp-area="" asp-controller="TicketTurno" asp-action="Index">
                                Buscar ticket
                            </a>
                        }
                        <div class="dropdown-divider"></div>
                        <a class="text-center nav-link text-dark" asp-area="" asp-controller="TicketTurno" asp-action="Create">
                            Registrar ticket
                        </a>

                        @if (SignInManager.IsSignedIn(User))
                        {
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header text-center">Catálogos</h6>
                            <div class="dropdown-divider"></div>
                            <a class="text-center nav-link text-dark" asp-area="" asp-controller="Municipio" asp-action="Index">
                                Municipios
                            </a>
                            <div class="dropdown-divider"></div>
                            <a class="text-center nav-link text-dark" asp-area="" asp-controller="NivelModels" asp-action="Index">
                                Niveles
                            </a>
                            <div class="dropdown-divider"></div>
                            <a class="text-center nav-link text-dark" asp-area="" asp-controller="Asunto" asp-action="Index">
                                Asuntos
                            </a>
                        }
                    </div>
                </div>
                <partial name="_LoginPartial.cshtml" />
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="p-4">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2022 - TicketParcial - <a id="register" asp-area="Identity" asp-page="/Account/Register">Registrar Nuevo Usuario</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/utils.js"></script>
    <script src="~/js/validations.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
